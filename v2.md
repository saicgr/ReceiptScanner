Extend the existing "ReceiptSnap" React Native app with V2 features. Keep the same architecture 
(offline-first SQLite, one-time purchase, Gemini Flash-Lite via the Render proxy).

FEATURE 1 — WARRANTY & RETURN TRACKING (the headline differentiator):
- When a receipt is extracted, have the Gemini prompt ALSO infer: 
  return_window_days and warranty_period (based on merchant + item type), returning them in the JSON.
- Let the user confirm/edit these on the review screen.
- Store a "protection status" per receipt and per high-value item.
- Schedule local push notifications (expo-notifications):
    * "Return window for {item} at {vendor} closes in 3 days" (3 days before expiry).
    * "Warranty for {item} expires in 30 days."
- Add a "Protections" tab listing active return windows and warranties, sorted by soonest expiry.
- Let users attach a photo of the product and store serial numbers for warranty claims.

FEATURE 2 — TAX-DEDUCTION INTELLIGENCE (locks in freelancers/SMBs):
- Add a "tax category" layer (e.g., Meals (50%), Supplies, Home Office, Mileage, Travel, Equipment).
- When extracting, have Gemini suggest a likely tax category and whether it's probably deductible; 
  user can override.
- Add a "Tax Report" generator: pick a date range / tax year, output a categorized summary 
  (totals per tax category, deductible vs non-deductible) as a Schedule C-style PDF/CSV.
- Add simple mileage logging and manual cash-expense entry so the expense record is complete 
  even without a paper receipt.

UX:
- Add an onboarding hint explaining the warranty reminders and tax features (these are the 
  reasons to keep the app installed).
- Keep everything editable; never auto-finalize AI guesses.

Update the Gemini prompt in the proxy to return the new fields, keep it backward-compatible 
with V1 receipts, and migrate the SQLite schema safely. Update the README.
